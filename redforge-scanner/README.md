# RedForge Scanner (React)

> RedForge 的 React 版本前端 - 早期原型開發版本

---

## 專案狀態

| 狀態 | 說明 |
|------|------|
| **開發狀態** | 🔒 已凍結（Archived） |
| **主要版本** | 請使用 [`redforge-scanner-vue`](../redforge-scanner-vue/) |
| **用途** | 早期原型驗證、技術概念驗證 |

> **注意**：此為 React 版本的早期原型，目前主要開發已轉移至 Vue 版本。如需進行開發，請使用 `redforge-scanner-vue`。

---

## 為什麼有兩個版本？

在專案初期，我們同時評估了 React 和 Vue 兩種技術方案：

| 版本 | 框架 | 狀態 | 說明 |
|------|------|------|------|
| `redforge-scanner` | React 19 | 凍結 | 早期原型，用於技術驗證 |
| `redforge-scanner-vue` | Vue 3 | **主要開發** | 正式版本，持續更新 |

最終選擇 Vue 3 的原因：
- 更好的 TypeScript 整合體驗
- Composition API 與 Pinia 的狀態管理更直觀
- 團隊對 Vue 生態系統更熟悉

---

## 技術棧

| 類別 | 技術 |
|------|------|
| **框架** | React 19.1.0 |
| **語言** | TypeScript 5.8 |
| **UI 框架** | Tailwind CSS 3.4 |
| **圖表** | Recharts 3.4 |
| **圖標** | Lucide React |
| **桌面框架** | Tauri 2 |
| **建置工具** | Vite 7.0 |

---

## 專案結構

```
redforge-scanner/
├── src/                          # 前端源碼
│   ├── App.tsx                   # 根組件
│   ├── App.css                   # 全域樣式
│   ├── main.tsx                  # 入口點
│   │
│   ├── components/               # React 組件
│   │   ├── Dashboard.tsx         # 主儀表板
│   │   ├── Scanner.tsx           # 掃描介面
│   │   └── ScanHistory.tsx       # 歷史記錄
│   │
│   ├── assets/                   # 靜態資源
│   └── styles/                   # 樣式檔案
│
├── src-tauri/                    # Rust 後端
│   ├── src/
│   │   ├── main.rs
│   │   ├── lib.rs
│   │   ├── commands/             # Tauri IPC 命令
│   │   ├── scanners/             # 掃描引擎
│   │   ├── models/               # 資料模型
│   │   └── database/             # 資料庫
│   └── Cargo.toml
│
├── dist/                         # 建置輸出
├── public/                       # 公開靜態檔案
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
└── postcss.config.js
```

---

## 核心組件

### Dashboard.tsx
主儀表板組件：
- 掃描統計概覽
- 漏洞分佈圓餅圖（使用 Recharts）
- 最近掃描列表
- 快速操作按鈕

### Scanner.tsx
掃描控制組件：
- 目標 URL 輸入
- 掃描類型選擇
- 即時掃描進度
- 結果預覽

### ScanHistory.tsx
歷史記錄組件：
- 掃描記錄表格
- 搜尋與篩選功能
- 報告匯出
- 詳細結果檢視

---

## 快速開始

> **建議**：除非有特殊需求，否則請使用 Vue 版本進行開發。

### 安裝依賴

```bash
npm install
```

### 開發模式

```bash
# 啟動 Tauri 開發伺服器
npm run tauri dev

# 僅啟動前端開發伺服器
npm run dev
```

### 建置

```bash
# 建置桌面應用
npm run tauri build

# 僅建置前端
npm run build
```

---

## 與 Vue 版本的差異

| 功能 | React 版本 | Vue 版本 |
|------|-----------|----------|
| 基礎掃描 | ✅ | ✅ |
| 歷史記錄 | ✅ | ✅ |
| 儀表板 | ✅ | ✅ |
| 狀態管理 | React useState | Pinia |
| 協作功能 | ❌ | ✅ |
| 匯入/匯出 | ❌ | ✅ |
| 加密功能 | ❌ | ✅ |
| WebSocket | ❌ | 🔨 開發中 |
| 持續維護 | ❌ | ✅ |

---

## 依賴套件

```json
{
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "clsx": "^2.1.1",
    "lucide-react": "^0.554.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "recharts": "^3.4.1",
    "tailwind-merge": "^3.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-react": "^4.6.0",
    "tailwindcss": "^3.4.17",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
```

---

## 法律聲明

**重要**：此工具僅供授權測試使用。未經授權對系統進行安全測試是違法的。

使用本工具前請確保：
- 已獲得目標系統的明確授權
- 了解並遵守當地法律法規
- 僅在合法的滲透測試場景中使用

---

## 相關專案

- [主專案 README](../README.md)
- [Vue 版本（主要開發）](../redforge-scanner-vue/README.md)

---

## 授權

MIT License - 詳見 [LICENSE](../LICENSE)
